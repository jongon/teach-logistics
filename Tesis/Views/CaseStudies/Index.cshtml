@model IEnumerable<Tesis.Models.CaseStudy>

@{
	ViewBag.Title = "Índice";
}

<div class="row wrapper border-bottom white-bg page-heading">
	<div class="col-sm-4">
		<h2>Índice</h2>
		<ol class="breadcrumb">
			<li class="active">
				<strong>Casos de estudio</strong>
			</li>
		</ol>
	</div>
</div>

<div class="wrapper wrapper-content animated fadeInRight">
	@Html.Flash()
	<div class="row">
		<div class="col-lg-12">
			<div class="ibox float-e-margins">
				<div class="ibox-title">
					<h5>Lista de casos de estudio</h5>
					<div class="ibox-tools">
						@Html.ActionLink("Crear Caso de estudio", "Create", null, new { @class = "btn btn-primary btn-xs" })
					</div>
				</div>
				<div class="ibox-content">
					<table id="caseStudy"class="table table-striped">
						<thead>
							<tr>
								<th>
									@Html.DisplayNameFor(model => model.Name)
								</th>
								<th>
									@Html.DisplayNameFor(model => model.Created)
								</th>
								<th>
									@Html.DisplayNameFor(model => model.Sections)
								</th>
								<th>
									Acción
								</th>
							</tr>
						</thead>
						<tbody>
						@foreach (var item in Model) {
							<tr>
								<td>
										@Html.DisplayFor(modelItem => item.Name)
								</td>
								<td>
										@Html.DisplayFor(modelItem => item.Created)
								</td>
								<td>
										@if (item.Sections.Count() > 0) {
											foreach (var sectionItem in item.Sections)
											{
												<span>Semestre: @sectionItem.Semester.Description</span>
												<span>Sección: @sectionItem.Number</span>
												<br>																																
											}
										} else {
											<span>No tiene secciones asociadas</span>
										}																														
								</td>
								<td>
									@Html.ActionLink("Detalles", "Details", new { id=item.Id }, new { @class = "btn btn-primary btn-sm"})
									@Html.ActionLink("Eliminar", "Delete", new { id = item.Id }, new { @class = "btn btn-danger btn-sm" })
								</td>
							</tr>
						}
						</tbody>
					</table>
				</div>
			</div>
		</div>
	</div>
</div>
@section Scripts {
	@Scripts.Render("~/bundles/CaseStudies/index")
}